name: Test sf-auth action

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  validate:
    name: Validate action YAML
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install action-validator
        run: npm install -g @action-validator/cli

      - name: Validate action.yml
        run: action-validator action.yml

  test-plugins:
    name: Test plugins installation
    runs-on: ubuntu-latest
    needs: check-secrets-sandbox
    if: needs.check-secrets-sandbox.outputs.has-secrets == 'true'
    steps:
      - uses: actions/checkout@v4

      - id: sf-auth
        uses: ./
        with:
          consumer-key: ${{ secrets.SF_CONSUMER_KEY_SANDBOX }}
          jwt-private-key: ${{ secrets.SF_JWT_PRIVATE_KEY_SANDBOX }}
          username: ${{ secrets.SF_USERNAME_SANDBOX }}
          org-alias: test-plugins
          plugins: '@salesforce/plugin-code-analyzer,sfdx-hardis'

      - name: Verify plugin-code-analyzer installed
        shell: bash
        run: sf plugins | grep "code-analyzer"

      - name: Verify sfdx-hardis installed
        shell: bash
        run: sf plugins | grep "sfdx-hardis"

  test-invalid-url:
    name: Negative test - invalid instance URL
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Should fail on http:// URL
        id: should-fail
        continue-on-error: true
        uses: ./
        with:
          consumer-key: fake-key
          jwt-private-key: fake-key
          username: fake@fake.com
          instance-url: http://login.salesforce.com

      - name: Assert step failed
        shell: bash
        run: |
          if [ "${{ steps.should-fail.outcome }}" != "failure" ]; then
            echo "Expected action to fail on http:// URL but it did not"
            exit 1
          fi
          echo "Correctly rejected invalid instance URL"

  check-secrets-sandbox:
    name: Check sandbox secrets
    runs-on: ubuntu-latest
    outputs:
      has-secrets: ${{ steps.check.outputs.has-secrets }}
    steps:
      - id: check
        env:
          SECRET: ${{ secrets.SF_CONSUMER_KEY_SANDBOX }}
        run: |
          if [ -n "$SECRET" ]; then
            echo "has-secrets=true" >> $GITHUB_OUTPUT
          else
            echo "has-secrets=false" >> $GITHUB_OUTPUT
          fi

  check-secrets-prod:
    name: Check production secrets
    runs-on: ubuntu-latest
    outputs:
      has-secrets: ${{ steps.check.outputs.has-secrets }}
    steps:
      - id: check
        env:
          SECRET: ${{ secrets.SF_CONSUMER_KEY_PROD }}
        run: |
          if [ -n "$SECRET" ]; then
            echo "has-secrets=true" >> $GITHUB_OUTPUT
          else
            echo "has-secrets=false" >> $GITHUB_OUTPUT
          fi

  check-secrets-mydomain:
    name: Check mydomain url secret
    runs-on: ubuntu-latest
    outputs:
      has-secrets: ${{ steps.check.outputs.has-secrets }}
    steps:
      - id: check
        env:
          SECRET: ${{ secrets.SF_INSTANCE_URL_MYDOMAIN }}
        run: |
          if [ -n "$SECRET" ]; then
            echo "has-secrets=true" >> $GITHUB_OUTPUT
          else
            echo "has-secrets=false" >> $GITHUB_OUTPUT
          fi

  check-secrets-auth-url:
    name: Check auth URL secret
    runs-on: ubuntu-latest
    outputs:
      has-secrets: ${{ steps.check.outputs.has-secrets }}
    steps:
      - id: check
        env:
          SECRET: ${{ secrets.SF_SFDX_AUTH_URL }}
        run: |
          if [ -n "$SECRET" ]; then
            echo "has-secrets=true" >> $GITHUB_OUTPUT
          else
            echo "has-secrets=false" >> $GITHUB_OUTPUT
          fi

  integration-sandbox:
    name: Integration test - sandbox JWT (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    needs: check-secrets-sandbox
    if: needs.check-secrets-sandbox.outputs.has-secrets == 'true'
    steps:
      - uses: actions/checkout@v4

      - id: sf-auth
        uses: ./
        with:
          consumer-key: ${{ secrets.SF_CONSUMER_KEY_SANDBOX }}
          jwt-private-key: ${{ secrets.SF_JWT_PRIVATE_KEY_SANDBOX }}
          username: ${{ secrets.SF_USERNAME_SANDBOX }}
          org-alias: test-sandbox
          get-org-info: 'true'

      - name: Verify authenticated org
        shell: bash
        run: |
          ORG_ID=$(sf org display --target-org ${{ steps.sf-auth.outputs.org-alias }} --json | jq -r '.result.id')
          echo "::add-mask::$ORG_ID"
          if [[ ! "$ORG_ID" =~ ^00D ]]; then
            echo "Authentication failed: org ID does not start with 00D"
            exit 1
          fi
          echo "Authentication confirmed"

      - name: Verify org-alias output matches input
        shell: bash
        run: |
          if [ "${{ steps.sf-auth.outputs.org-alias }}" != "test-sandbox" ]; then
            echo "Expected org-alias 'test-sandbox', got '${{ steps.sf-auth.outputs.org-alias }}'"
            exit 1
          fi

      - name: Verify outputs
        shell: bash
        run: |
          echo "::add-mask::${{ steps.sf-auth.outputs.my-domain-url }}"
          [[ ! "${{ steps.sf-auth.outputs.org-id }}" =~ ^00D ]] && echo "org-id does not start with 00D" && exit 1
          [ "${{ steps.sf-auth.outputs.type }}" != "Scratch" ] && echo "Expected type 'Scratch', got '${{ steps.sf-auth.outputs.type }}'" && exit 1
          [[ ! "${{ steps.sf-auth.outputs.my-domain-url }}" =~ ^https:// ]] && echo "my-domain-url is invalid" && exit 1
          [ "${{ steps.sf-auth.outputs.release-version }}" -lt 260 ] && echo "release-version seems invalid: ${{ steps.sf-auth.outputs.release-version }}" && exit 1
          echo "type: ${{ steps.sf-auth.outputs.type }}"
          echo "my-domain-url: ${{ steps.sf-auth.outputs.my-domain-url }}"
          echo "release-version: ${{ steps.sf-auth.outputs.release-version }}"

      - name: Verify private key was deleted
        if: always()
        shell: bash
        run: |
          if [ -f "$RUNNER_TEMP/server.key" ]; then
            echo "Private key was not cleaned up"
            exit 1
          fi
          echo "Private key correctly removed"

  integration-sandbox-auth-url:
    name: Integration test - prod auth-url (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    needs: check-secrets-auth-url
    if: needs.check-secrets-auth-url.outputs.has-secrets == 'true'
    steps:
      - uses: actions/checkout@v4

      - id: sf-auth
        uses: ./
        with:
          auth-method: auth-url
          sfdx-auth-url: ${{ secrets.SF_SFDX_AUTH_URL }}
          org-alias: test-sandbox-authurl
          get-org-info: 'true'

      - name: Verify authenticated org
        shell: bash
        run: |
          ORG_ID=$(sf org display --target-org test-sandbox-authurl --json | jq -r '.result.id')
          echo "::add-mask::$ORG_ID"
          if [[ ! "$ORG_ID" =~ ^00D ]]; then
            echo "Authentication failed: org ID does not start with 00D"
            exit 1
          fi
          echo "Authentication confirmed"

      - name: Verify org-alias output
        shell: bash
        run: |
          if [ "${{ steps.sf-auth.outputs.org-alias }}" != "test-sandbox-authurl" ]; then
            echo "Expected org-alias 'test-sandbox-authurl', got '${{ steps.sf-auth.outputs.org-alias }}'"
            exit 1
          fi
          echo "org-alias output correctly set"

      - name: Verify outputs
        shell: bash
        run: |
          echo "::add-mask::${{ steps.sf-auth.outputs.my-domain-url }}"
          [[ ! "${{ steps.sf-auth.outputs.org-id }}" =~ ^00D ]] && echo "org-id does not start with 00D" && exit 1
          [ "${{ steps.sf-auth.outputs.type }}" != "Production" ] && echo "Expected type 'Production', got '${{ steps.sf-auth.outputs.type }}'" && exit 1
          [[ ! "${{ steps.sf-auth.outputs.my-domain-url }}" =~ ^https:// ]] && echo "my-domain-url is invalid" && exit 1
          [ "${{ steps.sf-auth.outputs.release-version }}" -lt 260 ] && echo "release-version seems invalid: ${{ steps.sf-auth.outputs.release-version }}" && exit 1
          echo "type: ${{ steps.sf-auth.outputs.type }}"
          echo "my-domain-url: ${{ steps.sf-auth.outputs.my-domain-url }}"
          echo "release-version: ${{ steps.sf-auth.outputs.release-version }}"

  integration-debug-jwt:
    name: Integration test - debug JWT (ubuntu)
    runs-on: ubuntu-latest
    needs: check-secrets-sandbox
    if: needs.check-secrets-sandbox.outputs.has-secrets == 'true'
    steps:
      - uses: actions/checkout@v4

      - id: sf-auth
        uses: ./
        with:
          consumer-key: ${{ secrets.SF_CONSUMER_KEY_SANDBOX }}
          jwt-private-key: ${{ secrets.SF_JWT_PRIVATE_KEY_SANDBOX }}
          username: ${{ secrets.SF_USERNAME_SANDBOX }}
          org-alias: test-debug-jwt
          debug: 'true'
          get-org-info: 'true'

      - name: Verify authenticated org
        shell: bash
        run: |
          ORG_ID=$(sf org display --target-org test-debug-jwt --json | jq -r '.result.id')
          echo "::add-mask::$ORG_ID"
          if [[ ! "$ORG_ID" =~ ^00D ]]; then
            echo "Authentication failed: org ID does not start with 00D"
            exit 1
          fi
          echo "Authentication confirmed"

      - name: Verify org-alias output
        shell: bash
        run: |
          if [ "${{ steps.sf-auth.outputs.org-alias }}" != "test-debug-jwt" ]; then
            echo "Expected org-alias 'test-debug-jwt', got '${{ steps.sf-auth.outputs.org-alias }}'"
            exit 1
          fi
          echo "org-alias output correctly set"

      - name: Verify outputs
        shell: bash
        run: |
          echo "::add-mask::${{ steps.sf-auth.outputs.my-domain-url }}"
          [[ ! "${{ steps.sf-auth.outputs.org-id }}" =~ ^00D ]] && echo "org-id does not start with 00D" && exit 1
          [ "${{ steps.sf-auth.outputs.type }}" != "Scratch" ] && echo "Expected type 'Scratch', got '${{ steps.sf-auth.outputs.type }}'" && exit 1
          [[ ! "${{ steps.sf-auth.outputs.my-domain-url }}" =~ ^https:// ]] && echo "my-domain-url is invalid" && exit 1
          [ "${{ steps.sf-auth.outputs.release-version }}" -lt 260 ] && echo "release-version seems invalid: ${{ steps.sf-auth.outputs.release-version }}" && exit 1
          echo "type: ${{ steps.sf-auth.outputs.type }}"
          echo "my-domain-url: ${{ steps.sf-auth.outputs.my-domain-url }}"
          echo "release-version: ${{ steps.sf-auth.outputs.release-version }}"

      - name: Verify private key was deleted
        if: always()
        shell: bash
        run: |
          if [ -f "$RUNNER_TEMP/server.key" ]; then
            echo "Private key was not cleaned up"
            exit 1
          fi
          echo "Private key correctly removed"

  integration-debug-auth-url:
    name: Integration test - debug auth-url prod (ubuntu)
    runs-on: ubuntu-latest
    needs: check-secrets-auth-url
    if: needs.check-secrets-auth-url.outputs.has-secrets == 'true'
    steps:
      - uses: actions/checkout@v4

      - id: sf-auth
        uses: ./
        with:
          auth-method: auth-url
          sfdx-auth-url: ${{ secrets.SF_SFDX_AUTH_URL }}
          org-alias: test-debug-authurl
          debug: 'true'
          get-org-info: 'true'

      - name: Verify authenticated org
        shell: bash
        run: |
          ORG_ID=$(sf org display --target-org test-debug-authurl --json | jq -r '.result.id')
          echo "::add-mask::$ORG_ID"
          if [[ ! "$ORG_ID" =~ ^00D ]]; then
            echo "Authentication failed: org ID does not start with 00D"
            exit 1
          fi
          echo "Authentication confirmed"

      - name: Verify org-alias output
        shell: bash
        run: |
          if [ "${{ steps.sf-auth.outputs.org-alias }}" != "test-debug-authurl" ]; then
            echo "Expected org-alias 'test-debug-authurl', got '${{ steps.sf-auth.outputs.org-alias }}'"
            exit 1
          fi
          echo "org-alias output correctly set"

      - name: Verify outputs
        shell: bash
        run: |
          echo "::add-mask::${{ steps.sf-auth.outputs.my-domain-url }}"
          [[ ! "${{ steps.sf-auth.outputs.org-id }}" =~ ^00D ]] && echo "org-id does not start with 00D" && exit 1
          [ "${{ steps.sf-auth.outputs.type }}" != "Production" ] && echo "Expected type 'Production', got '${{ steps.sf-auth.outputs.type }}'" && exit 1
          [[ ! "${{ steps.sf-auth.outputs.my-domain-url }}" =~ ^https:// ]] && echo "my-domain-url is invalid" && exit 1
          [ "${{ steps.sf-auth.outputs.release-version }}" -lt 260 ] && echo "release-version seems invalid: ${{ steps.sf-auth.outputs.release-version }}" && exit 1
          echo "type: ${{ steps.sf-auth.outputs.type }}"
          echo "my-domain-url: ${{ steps.sf-auth.outputs.my-domain-url }}"
          echo "release-version: ${{ steps.sf-auth.outputs.release-version }}"

  integration-sandbox-pinned-version:
    name: Integration test - sandbox pinned (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    needs: check-secrets-sandbox
    if: needs.check-secrets-sandbox.outputs.has-secrets == 'true'
    steps:
      - uses: actions/checkout@v4

      - id: sf-auth
        uses: ./
        with:
          consumer-key: ${{ secrets.SF_CONSUMER_KEY_SANDBOX }}
          jwt-private-key: ${{ secrets.SF_JWT_PRIVATE_KEY_SANDBOX }}
          username: ${{ secrets.SF_USERNAME_SANDBOX }}
          sf-cli-version: 2.50.6
          org-alias: test-sandbox-pinned
          get-org-info: 'true'

      - name: Verify org-alias output
        shell: bash
        run: |
          if [ "${{ steps.sf-auth.outputs.org-alias }}" != "test-sandbox-pinned" ]; then
            echo "Expected org-alias 'test-sandbox-pinned', got '${{ steps.sf-auth.outputs.org-alias }}'"
            exit 1
          fi
          echo "org-alias output correctly set"

      - name: Verify CLI version
        shell: bash
        run: sf --version | grep "@salesforce/cli/2.50.6"

      - name: Verify authenticated org
        shell: bash
        run: |
          ORG_ID=$(sf org display --target-org ${{ steps.sf-auth.outputs.org-alias }} --json | jq -r '.result.id')
          echo "::add-mask::$ORG_ID"
          if [[ ! "$ORG_ID" =~ ^00D ]]; then
            echo "Authentication failed: org ID does not start with 00D"
            exit 1
          fi
          echo "Authentication confirmed"

      - name: Verify outputs
        shell: bash
        run: |
          echo "::add-mask::${{ steps.sf-auth.outputs.my-domain-url }}"
          [[ ! "${{ steps.sf-auth.outputs.org-id }}" =~ ^00D ]] && echo "org-id does not start with 00D" && exit 1
          [ "${{ steps.sf-auth.outputs.type }}" != "Scratch" ] && echo "Expected type 'Scratch', got '${{ steps.sf-auth.outputs.type }}'" && exit 1
          [[ ! "${{ steps.sf-auth.outputs.my-domain-url }}" =~ ^https:// ]] && echo "my-domain-url is invalid" && exit 1
          [ "${{ steps.sf-auth.outputs.release-version }}" -lt 260 ] && echo "release-version seems invalid: ${{ steps.sf-auth.outputs.release-version }}" && exit 1
          echo "type: ${{ steps.sf-auth.outputs.type }}"
          echo "my-domain-url: ${{ steps.sf-auth.outputs.my-domain-url }}"
          echo "release-version: ${{ steps.sf-auth.outputs.release-version }}"

      - name: Verify private key was deleted
        if: always()
        shell: bash
        run: |
          if [ -f "$RUNNER_TEMP/server.key" ]; then
            echo "Private key was not cleaned up"
            exit 1
          fi
          echo "Private key correctly removed"

  integration-production:
    name: Integration test - production (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    needs: check-secrets-prod
    if: needs.check-secrets-prod.outputs.has-secrets == 'true'
    steps:
      - uses: actions/checkout@v4

      - id: sf-auth
        uses: ./
        with:
          consumer-key: ${{ secrets.SF_CONSUMER_KEY_PROD }}
          jwt-private-key: ${{ secrets.SF_JWT_PRIVATE_KEY_PROD }}
          username: ${{ secrets.SF_USERNAME_PROD }}
          instance-url: https://login.salesforce.com
          org-alias: test-prod
          get-org-info: 'true'

      - name: Verify org-alias output
        shell: bash
        run: |
          if [ "${{ steps.sf-auth.outputs.org-alias }}" != "test-prod" ]; then
            echo "Expected org-alias 'test-prod', got '${{ steps.sf-auth.outputs.org-alias }}'"
            exit 1
          fi
          echo "org-alias output correctly set"

      - name: Verify authenticated org
        shell: bash
        run: |
          ORG_ID=$(sf org display --target-org ${{ steps.sf-auth.outputs.org-alias }} --json | jq -r '.result.id')
          echo "::add-mask::$ORG_ID"
          if [[ ! "$ORG_ID" =~ ^00D ]]; then
            echo "Authentication failed: org ID does not start with 00D"
            exit 1
          fi
          echo "Authentication confirmed"

      - name: Verify outputs
        shell: bash
        run: |
          echo "::add-mask::${{ steps.sf-auth.outputs.my-domain-url }}"
          [[ ! "${{ steps.sf-auth.outputs.org-id }}" =~ ^00D ]] && echo "org-id does not start with 00D" && exit 1
          [ "${{ steps.sf-auth.outputs.type }}" != "Production" ] && echo "Expected type 'Production', got '${{ steps.sf-auth.outputs.type }}'" && exit 1
          [[ ! "${{ steps.sf-auth.outputs.my-domain-url }}" =~ ^https:// ]] && echo "my-domain-url is invalid" && exit 1
          [ "${{ steps.sf-auth.outputs.release-version }}" -lt 260 ] && echo "release-version seems invalid: ${{ steps.sf-auth.outputs.release-version }}" && exit 1
          echo "type: ${{ steps.sf-auth.outputs.type }}"
          echo "my-domain-url: ${{ steps.sf-auth.outputs.my-domain-url }}"
          echo "release-version: ${{ steps.sf-auth.outputs.release-version }}"

      - name: Verify private key was deleted
        if: always()
        shell: bash
        run: |
          if [ -f "$RUNNER_TEMP/server.key" ]; then
            echo "Private key was not cleaned up"
            exit 1
          fi
          echo "Private key correctly removed"

  integration-mydomain:
    name: Integration test - My Domain (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    needs: check-secrets-mydomain
    if: needs.check-secrets-mydomain.outputs.has-secrets == 'true'
    steps:
      - uses: actions/checkout@v4

      - id: sf-auth
        uses: ./
        with:
          consumer-key: ${{ secrets.SF_CONSUMER_KEY_PROD }}
          jwt-private-key: ${{ secrets.SF_JWT_PRIVATE_KEY_PROD }}
          username: ${{ secrets.SF_USERNAME_PROD }}
          instance-url: ${{ secrets.SF_INSTANCE_URL_MYDOMAIN }}
          org-alias: test-mydomain
          get-org-info: 'true'

      - name: Verify org-alias output
        shell: bash
        run: |
          if [ "${{ steps.sf-auth.outputs.org-alias }}" != "test-mydomain" ]; then
            echo "Expected org-alias 'test-mydomain', got '${{ steps.sf-auth.outputs.org-alias }}'"
            exit 1
          fi
          echo "org-alias output correctly set"

      - name: Verify authenticated org
        shell: bash
        run: |
          ORG_ID=$(sf org display --target-org ${{ steps.sf-auth.outputs.org-alias }} --json | jq -r '.result.id')
          echo "::add-mask::$ORG_ID"
          if [[ ! "$ORG_ID" =~ ^00D ]]; then
            echo "Authentication failed: org ID does not start with 00D"
            exit 1
          fi
          echo "Authentication confirmed"

      - name: Verify outputs
        shell: bash
        run: |
          echo "::add-mask::${{ steps.sf-auth.outputs.my-domain-url }}"
          [[ ! "${{ steps.sf-auth.outputs.org-id }}" =~ ^00D ]] && echo "org-id does not start with 00D" && exit 1
          [ "${{ steps.sf-auth.outputs.type }}" != "Production" ] && echo "Expected type 'Production', got '${{ steps.sf-auth.outputs.type }}'" && exit 1
          [[ ! "${{ steps.sf-auth.outputs.my-domain-url }}" =~ ^https:// ]] && echo "my-domain-url is invalid" && exit 1
          [ "${{ steps.sf-auth.outputs.release-version }}" -lt 260 ] && echo "release-version seems invalid: ${{ steps.sf-auth.outputs.release-version }}" && exit 1
          echo "type: ${{ steps.sf-auth.outputs.type }}"
          echo "my-domain-url: ${{ steps.sf-auth.outputs.my-domain-url }}"
          echo "release-version: ${{ steps.sf-auth.outputs.release-version }}"

      - name: Verify private key was deleted
        if: always()
        shell: bash
        run: |
          if [ -f "$RUNNER_TEMP/server.key" ]; then
            echo "Private key was not cleaned up"
            exit 1
          fi
          echo "Private key correctly removed"
